# System Settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# ----------------------------------------
# ? BUILD VERSIONS (Centralized Control)
# ----------------------------------------
# Android 15 (Vanilla Ice Cream) - Stable
ANDROID_COMPILE_SDK=35
ANDROID_TARGET_SDK=35
ANDROID_MIN_SDK=24

# Java 17 is the new standard for Android Builds
JAVA_VERSION=17

# Kotlin Version
KOTLIN_VERSION=1.9.24

# ----------------------------------------
# ? LIBRARY IDENTITY (Edit per library)
# ----------------------------------------
LIB_GROUP_ID=dev.mgks.swv
LIB_ARTIFACT_ID=fcm-sync
LIB_VERSION=1.0.3
LIB_NAME=Android WebView FCM Sync
LIB_DESCRIPTION=A lightweight utility to sync FCM tokens into Android WebView cookies for targeted push notifications.
LIB_URL=https://github.com/mgks/android-webview-fcm-sync